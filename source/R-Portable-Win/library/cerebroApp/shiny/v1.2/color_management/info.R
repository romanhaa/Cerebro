##----------------------------------------------------------------------------##
## Tab: Color Management
##----------------------------------------------------------------------------##

colors_for_samples_info <- list(
  title = "Colors for samples",
  text = p("Using this interface, you can assign new colors to each of the samples which will then be used across all tabs in Cerebro.")
)

colors_for_clusters_info <- list(
  title = "Colors for clusters",
  text = p("Using this interface, you can assign new colors to each of the clusters which will then be used across all tabs in Cerebro.")
)
